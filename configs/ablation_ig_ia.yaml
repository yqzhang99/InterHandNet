# Ablation `+IG/IA` of Table III and Table IV: both interaction modules, without
# InterHand Temporal Fusion.
_base_: interhandnet.yaml

work_dir: work_dirs/ablation_ig_ia

model:
  use_interaction_graph: true
  use_interaction_attention: true
  use_interhand_temporal_fusion: false
