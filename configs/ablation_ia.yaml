# Ablation `+IA` of Table IV: Interaction Attention only.
_base_: base.yaml

work_dir: work_dirs/ablation_ia

model:
  use_interaction_graph: false
  use_interaction_attention: true
  use_interhand_temporal_fusion: false
