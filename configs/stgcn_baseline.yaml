# ST-GCN baseline: the backbone with all three proposed modules disabled.
# This is the `ST-GCN [18]` row of Table III.
_base_: base.yaml

work_dir: work_dirs/stgcn_baseline

model:
  use_interaction_graph: false
  use_interaction_attention: false
  use_interhand_temporal_fusion: false
