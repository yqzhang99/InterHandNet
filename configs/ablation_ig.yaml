# Ablation `+IG` of Table IV: Interaction Graph only.
_base_: base.yaml

work_dir: work_dirs/ablation_ig

model:
  use_interaction_graph: true
  use_interaction_attention: false
  use_interhand_temporal_fusion: false
